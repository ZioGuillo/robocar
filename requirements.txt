fastapi>=0.110.0
uvicorn>=0.27.0
jinja2>=3.1.3
python-multipart>=0.0.9
pydantic-settings>=2.2.0
httpx>=0.27.0
itsdangerous>=2.1.2
pytest>=8.0.0
pytest-asyncio>=0.23.0
# Pi hardware — import errors are caught gracefully on non-Pi systems
RPi.GPIO>=0.7.1
# picamera2 handles both CSI and USB cameras via libcamera — system package:
#   sudo apt install -y python3-picamera2
# tflite-runtime — no PyPI wheels for Python 3.13+ on ARM; install manually if needed:
#   pip install tflite-runtime  (Python <=3.11 only)
#   sudo apt install python3-tflite-runtime  (if available in apt)
# Without it, ml_driver.available = False and detection is silently disabled.
Pillow>=10.0.0
psutil>=5.9.0
prometheus-client>=0.20.0
